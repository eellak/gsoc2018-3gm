guage: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  # PyPy versions
  - "pypy3.5"
# command to install dependencies
install:
  - sudo pip install pymongo spacy numpy pytest

# command to run tests
script:
  - pytest src/tests.py
